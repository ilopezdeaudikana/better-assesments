# Three tech assesments combined
This app was created using `React`, `Express`, `Typescript`, `Zustand` and `Tailwind`.

## Available Scripts

### In the `/server` directory, you can run:

#### `npm start`

### In the `project` directory, you can run:

#### `npm run dev`


